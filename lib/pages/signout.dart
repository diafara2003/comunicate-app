import 'package:flutter/material.dart';

class SignOutPages extends StatelessWidget {
  const SignOutPages({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(),
    );
  }
}
